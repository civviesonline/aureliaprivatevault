{
  "name": "aurelia",
  "version": "0.1.0",
  "private": true,
  "description": "Aurelia full-stack workspace with frontend and backend apps.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "frontend:dev": "npm --prefix frontend run dev",
    "auth:frontend:dev": "npm --prefix auth-frontend run dev",
    "auth:frontend:build": "npm --prefix auth-frontend run build",
    "auth:backend:run": "mvn -f auth-backend/pom.xml spring-boot:run",
    "auth:backend:test": "mvn -f auth-backend/pom.xml test",
    "backend:dev": "npm.cmd --prefix backend run start:dev",
    "backend:start": "npm.cmd --prefix backend run start",
    "backend:build": "npm.cmd --prefix backend run build",
    "backend:test": "npm.cmd --prefix backend run test"
  },
  "engines": {
    "node": ">=20.11.0",
    "npm": ">=10.0.0"
  }
}
